# AUDIT_CLAIMS.md — Claim Ledger

**Audit date:** 2026-09-12 (day before judging)
**Method:** every falsifiable assertion in `problem.md`, `solution1.md`, `summary.md`, `README.md`, `docs/pitch/PITCH_DECK_PROMPT.md` and the 17-slide `.pptx` was checked against source. Docs were treated as untrusted. Where doc and code disagree, the code wins.

**Status key:** FALSE = contradicted by code · PARTIAL = a true core wrapped in an untrue claim · UNVERIFIABLE = no artifact exists to confirm or deny · TRUE = verified in source.

**Headline:** of 40 checked claims — **17 FALSE, 9 PARTIAL, 5 UNVERIFIABLE, 9 TRUE.**

---

## The one fact that decides most of this table

`app/build.gradle.kts:81-82` declares the app's only inference dependency:

```
implementation(libs.pytorch.android.lite)        // org.pytorch:pytorch_android_lite:1.13.0
implementation(libs.pytorch.torchvision.lite)
```
(`gradle/libs.versions.toml:14,43-44`)

A repo-wide grep of `app/`, `gradle/`, `build.gradle.kts`, `settings.gradle.kts` for `executorch|\.pte|QnnHtp|Qnn|NNAPI|Vulkan|Hexagon|HTP|delegate|GPU|DSP|XNNPACK|Genie|Llama` returns **zero code hits** — every match is English prose inside a comment. There is no delegate, no `.pte` loader, and no QNN library in the APK. `app/src/main/assets/` contains four files and none of them is a `.pte`.

**Silicon assignment, actual:**

| Model | Where it really runs | Evidence |
|---|---|---|
| YOLOv8n-Seg (4-class) | **ARM CPU** via PyTorch Lite interpreter | `YoloSegDefectSegmenter.kt:124` `LiteModuleLoader.load(path)`; no delegate set |
| "Tap CNN" | **ARM CPU** — and it is not a CNN. It is a 36-feature logistic regression: one scaler subtract/divide + 36 multiply-adds + a sigmoid, in pure Kotlin | `AcousticModelBundle.kt:44-53` |
| "Report LLM" | **ARM CPU** — and it is not a model. It is Kotlin string concatenation | `ReportGenerator.kt:66-75` |

**Nothing in this project has ever executed on a Hexagon NPU.**

---

## Ledger — worst first

| # | Claim (verbatim) | Doc:line | Status | Code evidence |
|---|---|---|---|---|
| 1 | "Executes all three on-device models: YOLOv8n-Seg INT8 (<30 ms), Tap CNN INT8 (<3 ms), and on-device report generation" on the Hexagon NPU | `solution1.md:44`; also `README.md:90`, `summary.md:106`, pptx s1/s8 | **FALSE** | `app/build.gradle.kts:81`; zero delegate hits app-wide; `YoloSegDefectSegmenter.kt:124`, `AcousticModelBundle.kt:44-53`, `ReportGenerator.kt:73` — all three on CPU |
| 2 | "Everything you saw ran on the Hexagon NPU" (spoken script) | `PITCH_DECK_PROMPT.md:128` | **FALSE** | as #1. This sentence is the single highest-risk line in the pitch |
| 3 | "on-device Llama 3.2 3B", "GenieX generates offline report live on-device", "~10 tok/s W4A16" | pptx s1/s3/s4/s5/s8/s10/s12; `README.md:90,221` | **FALSE** | `ReportGenerator.kt:61-75` — `synthesizeNarrative()` returns `"$count finding(s) recorded. Items: $labels. Severity breakdown: $severities."`. No GenieX/QAIRT/Llama dependency anywhere in `gradle/libs.versions.toml` |
| 4 | "tamper-evident, signed PDF", "SHA-256 fingerprint", "dual-party signature capture", "both parties sign on-screen" | `problem.md:20`, `solution1.md:27`, pptx s1/s4/s8/s12/s15 | **FALSE** | `ReportGenerator.kt:19-22` states it in its own words: *"'signed' here means a visible timestamp + session ID baked into the document, not a cryptographic signature"*. Grep for `MessageDigest\|KeyStore\|Signature\|sha256\|Cipher\|hmac` across `app/src/` → **zero hits**. No signature-capture UI exists (no `Canvas`/`Path` drawing surface) |
| 5 | "Pinhole Metric Engine: Area = (Mask Pixels × Z²)/(fx × fy) × 10.7639" | `solution1.md:6-7`, `summary.md:75-76`, pptx s6/s8/s16 | **FALSE (on device)** | Grep `focal\|fx\|fy\|intrinsic\|CameraCharacteristics\|LENS_\|depth\|10.7639` across `app/src/` → **zero hits**. Actual arithmetic: `WalkthroughScreen.kt:131` passes hardcoded `frameWidthInches = 48f, frameHeightInches = 36f`; `YoloSegDefectSegmenter.kt:52` computes `frameSqFt = (48/12)*(36/12) = 12.0`; `:59` returns `areaSqFtEstimate = coverageFraction * 12.0`. **No Z, no fx, no fy.** The number is mask-coverage × a constant, so it is identical whether the phone is 30 cm or 3 m from the wall. The real formula exists only in `ml/vision/predict.py:37-52`, which never runs on the phone, uses a hardcoded `focal_length_px = 500.0`, and takes Z from a manual Gradio slider (`ml/app.py:303-304`) |
| 6 | "Tap CNN INT8 <3 ms", "3-layer CNN, ~150 KB INT8 on the NPU", "ensembled with a Random Forest" | `solution1.md:44`, pptx s7/s10 | **FALSE** | Production path is `TrainedTapClassifier.kt:41-44` → `AcousticModelBundle.hollowProbability()` → logistic regression, 36 coefficients, CPU (`AcousticModelBundle.kt:44-53`). `acoustic_cnn.ptl` is loaded **only** by the unreachable bench (`PyTorchHelper.kt:27`) and its consumer `predictAudio()` (`:74-80`) returns a hardcoded string. `acoustic_tap_clf.joblib` is never loaded by Android at all |
| 7 | "Training set: 100+ own recordings (50 hollow, 50 solid) captured on the demo phone" | pptx s7 | **FALSE** | `ml/data/audio/` contains **8 files total** — 4 hollow, 4 solid, all named `WhatsApp Ptt 2026-09-06 at …ogg`, 31 KB combined. `app/src/main/assets/acoustic_tap_model.json` → `"trainedTaps": 15`. `ml/models/acoustic_report.json:2-5` → `n_taps 15, n_solid 8, n_hollow 7, n_recordings 8` |
| 8 | "Qualcomm Sensing Hub continuously listens for acoustic knuckle-tap transients without waking the main CPU/NPU" | `solution1.md:45`; `README.md:95`; pptx s7/s8/s9 | **FALSE** | No such API call exists in the repo. Actual capture: `AcousticTapClassifier.kt:54-60` opens `AudioRecord(MediaRecorder.AudioSource.MIC, …)` and blocks for 1.2 s, started by a button press (`WalkthroughScreen.kt:155-168`). Separately: Qualcomm's always-on/Sensing-Hub audio path is **not reachable by a non-privileged third-party app** — there is no public Android or Qualcomm SDK surface for it. This claim is not merely unimplemented, it is unimplementable as stated |
| 9 | "Zero Network Architecture … Guarantees by design that rental photos, tenant IDs, and inspection records never leave the physical handset" / "Every byte stays on the phone" | `solution1.md:52`, pptx s8/s15/s16 | **FALSE** | `AndroidManifest.xml:23` → `android:allowBackup="true"`. `backup_rules.xml:4` and `data_extraction_rules.xml:4` exclude **only** `smartlease.db`. Everything else in `filesDir` is in Google Auto Backup scope: the generated PDFs (`ReportGenerator.kt:120` writes `File(context.filesDir, "report_${sessionId}.pdf")`) and the 13.7 MB model copy (`YoloSegDefectSegmenter.kt:137-141`). The PDF itself prints *"Generated fully offline, on-device — no data left this phone."* (`ReportGenerator.kt:94`) on a file Android will upload to Google Drive. See exfiltration table below |
| 10 | "IR blaster … verifies operational status rather than just recording external appearance"; "the mic confirms the compressor starts, and the report records 'works'" | `problem.md:20`, `solution1.md:21`, pptx s4/s5/s8/s9 | **FALSE** | `IrController.kt:11-13` admits it: *"There is no public Android API to receive/decode an arbitrary IR signal (ConsumerIrManager is transmit-only)."* `WalkthroughScreen.kt:170-181`: success is logged purely because `manager.transmit()` returned without throwing (`IrController.kt:43-44`). **There is no feedback channel of any kind** — no mic listen-back, no thermal read, no current sense. The feature proves only that the phone's emitter fired. It cannot distinguish a working AC from an empty room |
| 11 | "Z from ARCore depth (±10%)" | pptx s6/s16 | **FALSE** | No ARCore dependency in `gradle/libs.versions.toml`; no `arcore`/`Session`/`sceneform` reference in `app/`. There is no depth source of any kind on device |
| 12 | "Dual Entry Points … Developer model benchmark validating raw tensor input/output shapes" | `solution1.md:29-31`, `summary.md:64-66` | **FALSE** | `AndroidManifest.xml:44-48` declares `com.iqoo.multimodal.MainActivity` with `exported="false"` and **no intent-filter**; nothing in `com.smartlease.edge` references it. It cannot be launched by a user. And it does not benchmark anything: `ImageModelScreen.kt:104` runs inference on `Bitmap.createBitmap(224,224,ARGB_8888)` — **a blank bitmap, not the camera** (its own comment at `:102` says *"In a real app, capture the image…"*); `AudioModelScreen.kt:69` passes `ShortArray(16000)` of zeros and never records (`:73` *"Placeholder for actual recording logic"*); `PyTorchHelper.kt:79` returns the literal string `"Audio Model Output: [Sound Y] (Placeholder)"` |
| 13 | "Fine-tuned YOLOv8n-Seg model (`best.pt` / **`vision_best.ptl`**)" | `solution1.md:4`, `summary.md:68` | **FALSE** | TorchScript inspection of `vision_best.ptl`: top-level class is `DetectionModel`, `forward(x) -> Tensor` (single tensor, no proto branch), and the head reshapes to **5** class channels. It is a 5-class **detection** model, not a 4-class segmentation model. It is structurally incompatible with `YoloSegDecoder` and with `YoloSegConfig.NUM_CLASSES = 4`. Only `yolov8n_seg.ptl` is the real segmentation model (`StaticYOLOv8SegWrapper`, `-> Tuple[Tensor,Tensor]`, 4 class channels + 32 coeffs) |
| 14 | "AR ghost overlay … re-align angles with move-in baseline photos within 1°" | `solution1.md:25`, pptx s5 | **FALSE** | `ArAlignmentTracker.kt:87` → `ALIGNMENT_TOLERANCE_DEG = 4f`, not 1°. Baseline is two in-memory floats (`:50-51`) destroyed on `onDispose` (`WalkthroughScreen.kt:66`). **No photo is ever saved**, so there is no move-in photo to ghost. There is no overlay rendering code — the "AR" surface is a one-line text label (`WalkthroughScreen.kt:112-116`) |
| 15 | "Room Database: **Encrypted** local inspection history" | `summary.md:115` | **FALSE** | `AppDatabase.kt:18-22` — plain `Room.databaseBuilder(...).build()`. No SQLCipher, no `SupportFactory`, no passphrase. The DB is a readable SQLite file |
| 16 | "Emits an immediate audible feedback tone confirming 'Solid' or 'Hollow'"; "Speaker confirms with a tone" | `solution1.md:50`, pptx s5/s7 | **FALSE** | Grep `ToneGenerator\|MediaPlayer\|SoundPool\|AudioTrack` across `app/` → zero hits. No audio is ever played |
| 17 | "Confidence < 0.30 hidden; 0.30–0.50 shown as 'review'. Users can dismiss a detection." | pptx s6/s16 | **FALSE** | `YoloSegConfig.kt:47` → `SCORE_THRESHOLD = 0.25f`, *below* the claimed hide threshold. `WalkthroughScreen.kt:135-142` logs **every** detection with raw confidence; there is no review band and no dismiss control anywhere in the UI |
| 18 | "Compiled INT8 ExecuTorch bundle … with Qualcomm runtime libraries targeting <30 ms" on the iQOO 15 | `solution1.md:5`, `summary.md:79-81` | **PARTIAL → misleading** | The `.pte` is genuine (magic `ET12` at offset 4) and the two `.so` are genuine aarch64 ELF. But: (a) it is **not in the APK** — it lives in `ml/YOLOV8/qnn_android_bundle/` and nothing loads it; (b) it was compiled for **SM8650 = Snapdragon 8 Gen 3** (`run_qnn_conversion.sh:48`, default at `convert_yolov8_seg_qnn_pte.py:86`), not the 8 Elite Gen 5 in the iQOO 15 — and `:170-173` silently falls back to SM8650 if the SoC name is unknown; (c) the bundle is **incomplete**: the script's own `needed_libs` list (`:215-220`) requires `libQnnHtpV75Skel.so` / `libQnnHtpV73Skel.so` / `libqnn_executorch_backend.so`, none of which are present. Without a Skel library the HTP cannot execute |
| 19 | "<30 ms inference", "<3 ms tap CNN", "~15 s report stream" | `solution1.md:5,44`; `summary.md:81`; pptx s3/s5/s6/s7/s10 | **UNVERIFIABLE — no measurement exists** | There is no latency log, no benchmark harness, no `Trace`/`SystemClock` timing call anywhere in `app/`. No on-device run has ever been recorded. To the project's partial credit, `PITCH_DECK_PROMPT.md:115` carries an unchecked TODO acknowledging these are targets — but slides 3, 5, 8 and 10 present them without that qualifier |
| 20 | "mAP@50, mAP@50-95, loss curves, and confusion matrix" are produced; "Target metrics: 75%+ mAP" | `ml/YOLOV8/README.md:65`, pptx s6 | **UNVERIFIABLE — no metric of any kind exists** | `find` for `results*.csv`, `*confusion*`, `*PR_curve*`, `runs/`, `args.yaml`, `*.png` across `YOLOV8/` → **zero results**. `smartlease_edge_yolov8_seg.ipynb` has **0 saved outputs and every `execution_count` is `null`** — including its own §5 "Validation & Quantitative Metrics" cells (17-18). `best.pt` is a 6.78 MB binary with no provenance record whatsoever |
| 21 | INT8-vs-FP32 accuracy delta for the quantised model | pptx s6/s10 implied | **UNVERIFIABLE** | Never measured. Additionally the calibration preprocessing is wrong: `prepare_calibration.py:39` does a plain `cv2.resize` to 640×640 (aspect-distorting), while the Android inference path letterboxes with 114/255 grey padding (`YoloSegDefectSegmenter.kt:82,111`). The INT8 scales were calibrated on a distribution the model will never see |
| 22 | "Fine-tuned on **580+** labelled wall-defect images. Classes: crack, stain, mould, peeling, **hole**" | pptx s6 | **FALSE (both halves)** | Actual: 1796 train / 203 val / 107 test = **2106** images (`unified_defects/`). Actual classes: `crack, peeling, spalling, stain_mould` (`unified_defects/data.yaml`, mirrored in `YoloSegConfig.kt:33`). There is no `hole` class and `stain`/`mould` are one class, not two |
| 23 | "Scikit-learn Random Forest model (`acoustic_tap_clf.joblib`)" listed as an implemented on-device model | `solution1.md:16` | **FALSE** | Never referenced by any Kotlin file. `ml/models/acoustic_report.json:9` selects `LogisticRegression` as `best_model`; the RF was evaluated and discarded |
| 24 | "Severity tiers (Minor/Moderate/Severe) and automated repair cost estimates based on Chennai plaster/paint rates" | `solution1.md:8`, `summary.md:77`, pptx s4 | **FALSE (on device)** | No severity tiering and no cost estimate exists in `app/`. `Severity` (`InspectionEntity.kt:31-35`) is `INFO/NOTABLE/STOP_ESCALATE` and only ever set by the hazard keyword gate (`WalkthroughScreen.kt:72`) — **`NOTABLE` is never assigned by any code path**. Cost tables exist only in `ml/report/templates.py:12-32`, Python-only |
| 25 | "Local Storage: SQLite Room database with backup exclusion rules" | `solution1.md:28`, `README.md:119` | **PARTIAL** | True for the DB (`backup_rules.xml:4`). False as an implied whole-app property — see #9 |
| 26 | "100% offline / no cloud API / no `android.permission.INTERNET`" | `solution1.md:52`, `problem.md:10,20`, pptx s16 | **PARTIAL — TRUE as literally stated** | `AndroidManifest.xml` declares no `INTERNET` permission. ML Kit is the **bundled** Latin recognizer (`mlkit-text-recognition:16.0.1`, `libs.versions.toml:10`), genuinely on-device. But the app's *data* is not offline — see #9. "No INTERNET permission" and "data never leaves the handset" are not the same statement, and the deck uses the first to prove the second |

> **Claim 26 superseded on 2026-09-12.** The app now declares `INTERNET` and `ACCESS_NETWORK_STATE` for real: the product owner decided it should sync signed reports to a backend (`com.smartlease.edge.sync`, server in `server/`). Sync ships **off by default**, but the permission is present, so *"no `android.permission.INTERNET`"* must not be claimed anywhere from now on. The narrower claim that survives, and that is structural rather than a promise: inspection **media** never leaves the device — the sync wire types have no field able to hold image/audio bytes, the server schema has no binary column, and the API rejects unknown fields (verified live: a payload carrying `photoBase64` is refused with `422 Extra inputs are not permitted`). What can cross the wire is a SHA-256 digest and rupee totals. For the record, the original claim was true when written: measured the same day, the pre-change merged manifest contained zero `INTERNET` entries and `dumpsys package` on the installed build listed only `TRANSMIT_IR`, `CAMERA`, `RECORD_AUDIO` and an internal broadcast permission — ML Kit's `transport-backend-cct` did request both permissions, and `tools:node="remove"` successfully stripped them.

| 27 | "17-slide presentation deck designed specifically against the iQOO 2026 judging rubric" | `summary.md:92` | **PARTIAL** | 17 slides confirmed, rubric mapping on s14 confirmed. But the deck **still contains unfilled placeholders on judging day**: s1 `[Team name] · [Member 1] · [Member 2] · [Member 3]` and `[Student / Professional bucket]`; s14 `[Member 1]/[Member 2]/[Member 3]`; s15 `[email]`. s1 and s4 also carry the caption *"Target UI · app build in progress (Days 6–10)"* — the product screenshots are mockups |
| 28 | s12 "Shipping before the battle (Days 3–10)" — all six items ticked ✓, including "GenieX Llama 3.2 3B narrative" and "SHA-256 fingerprint + dual signature" | pptx s12 | **FALSE** | Today *is* the battle. Neither exists in code. A ✓ next to a thing that does not exist, on a slide titled "Working MVP, **Honestly**", is the most quotable contradiction in the submission |
| 29 | "We are not pitching an idea. We are showing you a working system." | pptx s15 | **PARTIAL** | A real app does build and run. But four of the five bullets directly beneath that sentence on the same slide are false (#1, #3, #4, #9) |
| 30 | "Interactive Python ML & Testing Hub — standalone Gradio app" | `solution1.md:33-34`, `summary.md:86` | **TRUE** | `ml/app.py`, `launch(share=False)` at `:452-455`. Genuinely local |
| 31 | "Offline OCR — Google ML Kit text recognition" | `solution1.md:26`, `README.md:114` | **TRUE** | `OcrEngine.kt:18` `TextRecognition.getClient(TextRecognizerOptions.DEFAULT_OPTIONS)` with the bundled artifact |
| 32 | "Real ConsumerIrManager wrapper transmitting 38 kHz carrier bursts" | `solution1.md:20`, `README.md:115` | **TRUE** | `IrController.kt:24,36-47`. Carrier comes from `CommonAcIrProfiles.profiles` (`:66-72`), all 38000 Hz, each labelled *"verify on-site"* |
| 33 | "Real CameraX wrapper, binds to device rear camera" | `README.md:112` | **TRUE** | `CameraController.kt:29-44` |
| 34 | "Real SensorManager rotation-vector tracking, pitch/roll delta from baseline" | `README.md:113` | **TRUE** | `ArAlignmentTracker.kt:54-79` |
| 35 | "Real AudioRecord capture, real FFT, amplitude/decay heuristic" | `README.md:116` | **TRUE** | `AcousticTapClassifier.kt:49-146`, `Fft.kt` — a self-contained radix-2 FFT, no third-party DSP |
| 36 | "Real deterministic keyword-based rule engine" (safety gate) | `README.md:117` | **TRUE** | `SafetyGate.kt:21-47`. Genuinely deterministic, genuinely non-ML, genuinely auditable in 20 lines. This is the most defensible design decision in the repo |
| 37 | "Real offline PDF rendering (Android PdfDocument API)" | `README.md:118` | **TRUE** | `ReportGenerator.kt:77-124`. Renders and writes a real A4 PDF, no network |
| 38 | Acoustic model quality: "cross-validated 80% (95% CI 55–93%)" surfaced in-app | `TrainedTapClassifier.kt:54-57` | **TRUE and honestly reported** | `acoustic_tap_model.json` → `groupedAccuracy 0.8`, `groupedAccuracyCi95 [0.548, 0.930]`, `trainedTaps 15`. Produced by `LeaveOneGroupOut` grouped by source recording (`export_for_android.py:72-84`), which correctly prevents sibling-tap leakage. `ml/models/acoustic_report.json:8` records `majority_baseline 0.533`. The app quotes the cross-validated figure with its interval, not the 100% training-set score, and a unit test guards against that regression (`AcousticFeatureExtractorTest.kt:117`). **This is the one place the project's engineering discipline is genuinely exemplary — and it is built on 15 taps from 8 WhatsApp voice notes** |
| 39 | Python↔Kotlin acoustic feature parity is test-enforced | `AcousticFeatureExtractor.kt:23-24` | **TRUE** | `AcousticFeatureExtractorTest.kt:75-95` checks all 36 features and the final probability against golden values emitted by `export_for_android.py:117-123`. Real train/serve-skew defence |
| 40 | `yolov8n_seg.ptl` is a genuine fine-tuned 4-class YOLOv8n-Seg | `solution1.md:4` | **TRUE (structure) / UNVERIFIABLE (quality)** | TorchScript confirms `StaticYOLOv8SegWrapper` → `Tuple[Tensor,Tensor]`, 4 class channels + 32 mask coeffs + `Proto` head; matches `YoloSegConfig` exactly. But see #20 — no metric exists to say whether it detects anything useful |

---

## Exfiltration table — per path, does Auto Backup take it?

`android:allowBackup="true"` (`AndroidManifest.xml:23`) with `fullBackupContent`/`dataExtractionRules` excluding only `smartlease.db`.

| Artifact | Written at | Path | Auto Backup uploads it? |
|---|---|---|---|
| Inspection findings DB | `AppDatabase.kt:18-22` | `databases/smartlease.db` | **No** — explicitly excluded (`backup_rules.xml:4`, `data_extraction_rules.xml:4`). Note `-wal`/`-shm` siblings are *not* named in the exclusion |
| Generated PDF report | `ReportGenerator.kt:120-121` | `filesDir/report_<sessionId>.pdf` | **YES → Google Drive.** This is the tenant-facing evidence artifact, and it is the one thing the claim says never leaves the phone |
| Copied ML model (13.7 MB) | `YoloSegDefectSegmenter.kt:137-141` | `filesDir/yolov8n_seg.ptl` | **YES** — plus `vision_best.ptl` and `acoustic_cnn.ptl` if the bench ever ran (`PyTorchHelper.kt:83-98`) |
| Captured photos | — | **nowhere** | N/A — **no photo is ever saved to disk.** `CameraController.captureBitmap()` returns a `Bitmap` that is analysed and garbage-collected (`WalkthroughScreen.kt:126-142`) |
| Audio tap clips | — | **nowhere** | N/A — PCM stays in a `ShortArray` and is discarded (`AcousticTapClassifier.kt:63-78`) |

**The two rows that read "nowhere" are a bigger problem than the two that read "YES".** A security-deposit evidence tool that retains no photograph and no audio clip has no evidence.

## Permissions vs. the privacy claim

| Permission | Declared | Actually used? |
|---|---|---|
| `CAMERA` | `AndroidManifest.xml:6` | Yes — `CameraController.kt` |
| `RECORD_AUDIO` | `:9` | Yes — `AcousticTapClassifier.kt:54` |
| `ACCESS_FINE_LOCATION` | `:17`, requested at `MainActivity.kt:37` | **No.** `InspectionEntity.latitude/longitude` (`:19-20`) default to `null` and **no code path ever sets them**. The app asks a user for precise location on first launch and then throws it away |
| `VIBRATE` | `:20` (comment: "haptic confirmation on Tier-1 flags") | **No.** Zero `Vibrator`/`vibrate` references |

Two of four requested permissions are unused. On a slide that argues "private by physics", an unused `ACCESS_FINE_LOCATION` prompt is the first thing a privacy-minded judge will notice.
