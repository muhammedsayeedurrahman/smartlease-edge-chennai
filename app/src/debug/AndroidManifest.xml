<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only manifest fragment, merged only into debug builds. Points the application at
  network_security_config_debug.xml so a debug build can reach a backend on the developer's
  own machine over plain HTTP via `adb reverse`. Never merged into release.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config_debug" />
</manifest>
